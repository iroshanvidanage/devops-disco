---

defaults:
  datadir: data
  lookup_key: eyaml_lookup_key
  options:
    pkcs7_private_key: /etc/puppet/keys/private_key.pkcs7.pem
    pkcs7_public_key: /etc/puppet/keys/public_key.pkcs7.pem

hierarchy:
  - name: "Secret data"
    paths:
      - "secret/%{facts.datacenter}.yaml"
      - "secret.yaml"