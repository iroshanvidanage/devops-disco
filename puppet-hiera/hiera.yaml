---

version: 5

defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: "Global data"
    path: "common.yaml"
  - name: "Per-node data"
    path: "nodes/%{fact.fqdn}.yaml"
  - name: "Per-role data"
    path: "roles/%{fact.role}.yaml"